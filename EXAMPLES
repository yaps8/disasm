Output:
Information regarding the disassembly and the file.dot graph (open it with xdot for instance)

Syntax:
python disasm.py file first_addr [last_addr] [offset] [entrypoint] [trace_list] [trace_detailled] [bin/dump] [usetrace/displaytrace] [verbose]
Optional parameters can be skipped with / or 0 for last_addr, offset and entrypoint (if they can be inferred from trace).

The following color and shapes apply for nodes and edges in the file.dot graph.

Nodes:
  Orange -> entrypoint (into trace)
  Blue -> exitpoint (from trace)
  Pink -> node in trace
  White -> node not in trace (static analysis)
Directed edges:
  Color:
    Red -> Jump target
    Black -> Next instruction
  Shape:
    Dashed -> From static analysis only
    Solid -> From trace only
    Bold -> Both
Non-directed edges:
  Black dotted edges -> Overlapping insructions:



Manual example:
Disasm without trace:
python2 disasm.py manual_ex/running-ex -b 0x8048060 -l 0x80480ae -o 0 -e 0x8048060

Disasm with trace:
python2 disasm.py manual_ex/running-ex 0x8048060 0 0 / manual_ex/running-ex_itrace.out

Displaying the layers:
python2 disasm.py manual_ex/running-ex 0x8048060 0 0 / manual_ex/running-ex_itrace.out / / / verbose


TELocK:
Disasm without trace:
python2 disasm.py telock/telock99-hostname.bin.exe.snapshot2 0x01000000 0 0x01000000 0x01006e5b / / dump

Disasm with trace:
It is needed to unzip telock's trace: unzip telock/trace3.zip
python2 disasm.py telock/telock99-hostname.bin.exe.snapshot2 0x01000000 0 0x01000000 / telock/trace3-addr-ordre02 telock/trace3 dump usetrace


UPX:
Disasm without trace:
python2 disasm.py upx/upx-hostname.exe.bin.snapshot0 0x01000000 0 0x01000000 0x010058c0 / / dump 
python2 disasm.py upx/upx-hostname.exe.bin.snapshot1 0x01000000 0 0x01000000 0x010011d7 / / dump 

Disasm with trace:
python2 disasm.py upx/upx-hostname.exe.bin.snapshot0 0x01000000 0 0x01000000 / upx/upx_addr_ordre_00 upx/upx-hostname.exe.bin.trace dump usetrace
python2 disasm.py upx/upx-hostname.exe.bin.snapshot1 0x01000000 0 0x01000000 / upx/upx_addr_ordre_01 upx/upx-hostname.exe.bin.trace dump usetrace


Sane examples:
ls:
python2 disasm.py sane/ls 0x08048000 / 0 / sane/ls_itrace.out

mkdir:
python2 disasm.py sane/mkdir 0x08048000 / 0 / sane/mkdir_itrace.out

id:
python2 disasm.py sane/id 0x08048000 / 0 / sane/id_itrace.out
